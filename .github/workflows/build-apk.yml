name: Build NoorPrayer APK

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Extract project
        run: unzip -o NoorPrayer-V0.1-GitHubBuild.zip

      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Set up Android
        uses: android-actions/setup-android@v3

      - name: Install Android SDK
        run: sdkmanager "platforms;android-35" "build-tools;35.0.0"

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.9'

      - name: Build APK
        run: gradle -p NoorPrayer assembleDebug

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: NoorPrayer-APK
          path: NoorPrayer/app/build/outputs/apk/debug/app-debug.apk
